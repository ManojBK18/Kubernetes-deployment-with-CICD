server {
        listen 80;

        root  /app;
        index index.html;

        # Serve React static files
        # Fallback to index.html for React Router support
        location / {
            try_files $uri $uri/ /index.html;
        }
}