apiversion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: url-shortener
  labels:
    app: postgres
spec:
  clusterIP: None
  selector:
    app: postgres
    ports:
      - port: 5432
        targetPort: 5432