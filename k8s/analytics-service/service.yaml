apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: url-shortener
  labels:
    app: analytics
spec:
  type: clusterIP
  selector:
    app: analytics
  ports:
    - port: 8080
      targetPort: 8080