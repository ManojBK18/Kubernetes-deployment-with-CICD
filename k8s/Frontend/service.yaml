apiersion: 1
kind: Service
metadata:
  name: frontend
  namespace: url-shortener
  labels:
    app: frontend
spec:
  type: clusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80