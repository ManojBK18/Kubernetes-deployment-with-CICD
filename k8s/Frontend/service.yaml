apiersion: 1
kind: Service
metadata:
  name: shortener-service
  namespace: url-shortener
  labels:
    app: shortener
spec:
  type: clusterIP
  selector:
    app: shortner
  ports:
    - port: 80
      targetPort: 80